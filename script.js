  //let a = 10;
  //for (let i = 0; i < a; i++) {
  //  console.log(i)
  //}

  //cosole log always print in a single line
  // so  to print the numbers in sigle line 
  //use the string for eaxample if we want to print these number in asingle line  write the following code
  // write a program o print numbers from 0 to 30

  /*let a = 30;
  let result = "";
  for (let i = 0; i < a; i++) {
      result += i + " ";

  }
  console.log(result)*/
  // in some ide let is not suported like webstorm
  // so just go o setting and enable the ecmascript6
  //it is the current standard latest version of nodejs
  //basic syntax for  for varible  declaration
  var a = 10;
  var b = 20; // semicolon are  opional
  // in ecma script we got two new keywords
  //called let and const
  let c = 30;
  const d = 40;
  c++;
  d++;/ it is no possible because d is  constant  and  value of constant can not be changed
  
  console.log(c)
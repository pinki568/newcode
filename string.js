let p = 'pinki'
let str = 'this is a string'

// searching in string
//let s = 'this has some data here'
let key = 'have'
    //console.log(s.indexOf(key)) // f a string is present then the index of sting is obtained but if not then -1 will be the output
    // it is a good way to check a string is contain or not
let s = 'this is easy job'
console.log(s.indexOf('is', 3))